/*	header for the varibels of pass2 functions	*/

#define fstArg args[0] /* the first argument of the line */
#define secArg args[1] /* the second argument of the line */
#define fstPar params[0] /* the first parameter of the operand */
#define secPar params[1] /* the second parameter of the operand  */

